apiVersion: v1
kind: Service
metadata:
  name: python-webapp-service-2
spec:
  ports:
    - port: 6000
      protocol: TCP
      targetPort: 6000
  # the selector is used to link pods to services
  selector:
    app: python-webapp-app-2
  type: NodePort
