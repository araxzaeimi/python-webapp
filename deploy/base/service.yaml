apiVersion: v1
kind: Service
metadata:
  name: python-webapp-service
spec:
  type: LoadBalancer
  selector:
    app: python-webapp-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
